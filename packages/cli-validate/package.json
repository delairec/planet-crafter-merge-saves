{
  "name": "cli-validate",
  "version": "1.0.0",
  "description": "Validate Planet Crafter save files against a JSON schema",
  "type": "module",
  "main": "cli/validate-cli.js",
  "scripts": {
    "test": "bun --cwd ../.. test packages/cli-validate",
    "test:watch": "bun --cwd ../.. test --watch packages/cli-validate",
    "lint:types": "tsc --noEmit",
    "validate": "bun --cwd ../.. packages/cli-validate/cli/validate-cli.js ",
    "validate:test": "bun test packages/cli-validate",
    "node:validate": "node cli/validate-cli.js --platform=node"
  },
  "dependencies": {
    "core-mapping": "workspace:*",
    "shared-platforms": "workspace:*",
    "shared-save-processing": "workspace:*"
  }
}
